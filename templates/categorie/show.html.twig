{% extends 'back-office.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <div class="card">
        <h1 class="text-center">Modify Category</h1>

<div class="card">
    <p class="card-text">
        <label>Nom Categorie:</label> {{ categorie.nomcategorie }}
    </p>
    <div class="card-footer">
        <a href="{{ path('app_categorie_index') }}" class="btn btn-secondary">Back</a>
        <div class="float-right">
            <a href="{{ path('app_categorie_edit', {'idcategorie': categorie.idcategorie}) }}" class="btn btn-primary">Edit</a>
            {{ include('categorie/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}